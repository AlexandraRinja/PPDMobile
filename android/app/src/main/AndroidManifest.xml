<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alis.exemplu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegisterActivity"
            android:label="@string/title_activity_main">
        </activity>
        <activity
            android:name=".ui.SuccessActivity"
            android:label="@string/title_activity_success">
        </activity>

        <activity
            android:name=".ui.UserListActivity"
            android:label="User test">
        </activity>

        <activity
            android:name=".ui.LoginActivity"
            android:label="LifeSum app">
        </activity>

        <activity
            android:name=".ui.RecipesNewActivity"
            android:label="LifeSum app">
        </activity>

        <activity
            android:name=".ui.DetailActivity1"
            android:label="LifeSum app">
        </activity>

        <activity
            android:name=".ui.DetailActivity0"
            android:label="LifeSum app">
        </activity>
    </application>

</manifest>